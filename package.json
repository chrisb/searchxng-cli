{
  "name": "searchxng-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "pack:cli": "npm pack --workspace searchxng --dry-run",
    "pack:pi": "npm pack --workspace @chrisbielinski/pi-searchxng --dry-run"
  }
}
